import*as THREE from"./three.module.min.js";import{OrbitControls}from"./OrbitControls.js";const scene=new THREE.Scene,camera=new THREE.PerspectiveCamera(60,window.innerWidth/window.innerHeight,1,1e3);camera.position.set(0,5,10),camera.lookAt(scene.position);const renderer=new THREE.WebGLRenderer({antialias:!0});renderer.setSize(window.innerWidth,window.innerHeight),document.body.appendChild(renderer.domElement);const controls=new OrbitControls(camera,renderer.domElement),geom=new THREE.CylinderGeometry(3,3,5,5,1);geom.rotateX(1*-Math.PI);const cylinder=new THREE.Mesh(geom,new THREE.MeshBasicMaterial({color:"aqua",wireframe:!0}));function render(){requestAnimationFrame(render),renderer.render(scene,camera)}scene.add(cylinder),render();